import { Routes } from "@angular/router";
import { CompilersPage } from "./compilers";

export const COMPILERS_ROUTES: Routes = [
  { path: "topics/compilers", component: CompilersPage, title: "Compiler Design · CS" },
];
