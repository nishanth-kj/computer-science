import { Routes } from "@angular/router";
import { ParallelPage } from "./parallel";

export const PARALLEL_ROUTES: Routes = [
  { path: "topics/parallel", component: ParallelPage, title: "Parallel & Concurrent Computing · CS" },
];
